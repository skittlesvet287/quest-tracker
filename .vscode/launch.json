{
    "configurations": [
        
    {
        "name": "Attach to Integrated Browser",
        "request": "attach",
        "type": "editor-browser",
        "webRoot": "${workspaceFolder}"
    }
    ]
}